class Solution {
    
    private boolean canIPlace(char[][] board, int currentCellValue){
        // In Same Row
        // In Same Col
        // Sub Matrix % 3
        // true / false
    }
    public void solveSudoku(char[][] board) {
        // 
        for(int col = 0; col<board[row].length; col++){}
        if(canIPlace(board, board[row][col])){
            
}
    }
}
